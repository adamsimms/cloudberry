
# Please run the following commands:

cd "directory where file is downloaded"
tar xfvz camerasuitepi.tar.gz
cd camerasuite
./camerasuite.sh -platform xcb


# If there are missing dependencies then install the following packages:
sudo apt-get update
sudo apt-get upgrade
# If you do not run the latest Raspbian version then a dist-upgrade might be necessary:
# sudo apt-get dist-upgrade
sudo apt-get install openssl sqlite libts-0.0 libinput5 libgles2-mesa libstdc++6 libgcc1 \
libc6 libegl1-mesa libegl1-mesa-drivers libexpat1 libz1 libpng12-0 libevdev2 libpcre3 \
libxdmcp6 libxau6 libfreetype6 libfontconfig1 libmtdev1 libudev1 libxkbcommon0 libglib2.0-0 \
libx11-6 libx11-xcb1 libxext6 libts-0.0-0 libxcb1 libdbus-1.3

# Optional:
sudo apt-get install gstreamer1.0-omx libgstreamer1.0-dev libgstreamer-plugins-base1.0
