name: Refresh WittyPi manual

on:
  schedule:
    - cron: "0 6 1 * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh-manual:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download latest WittyPi manual
        run: |
          curl -fsSL -o wittypi/wittypi2-manual.pdf \
            http://www.uugear.com/doc/WittyPi2_UserManual.pdf
          file wittypi/wittypi2-manual.pdf

      - name: Commit if changed
        run: |
          if git diff --quiet -- wittypi/wittypi2-manual.pdf; then
            echo "Manual unchanged"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add wittypi/wittypi2-manual.pdf
          git commit -m "Refresh WittyPi manual from vendor"
          git push
